<Window x:Class="FusionPortalClient.EnrollmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FusionPortal Client - Device Enrollment"
        Width="380" Height="260"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Enroll this device" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" />

        <TextBlock Grid.Row="1" Text="Site / org code" Margin="0,0,0,4" />
        <TextBox Grid.Row="2" x:Name="SiteCodeBox" Margin="0,0,0,10" />

        <TextBlock Grid.Row="3" Text="Enrollment code (from your coach/admin)" Margin="0,0,0,4" />
        <TextBox Grid.Row="4" x:Name="EnrollmentCodeBox" Margin="0,0,0,10" />

        <TextBlock Grid.Row="5" x:Name="ErrorText" Foreground="Red" TextWrapping="Wrap" VerticalAlignment="Top" />

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancel" Width="100" Margin="0,0,10,0" Click="Cancel_Click" />
            <Button Content="Enroll" Width="100" Click="Enroll_Click" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
